# Contexto de build = backend/
# Python
# `**/` para que matchee también los subdirectorios (app/**/__pycache__,
# ml/__pycache__): sin el `**` solo excluía el __pycache__ de la raíz.
**/__pycache__/
*.py[cod]
*.pyo
.pytest_cache/

# Secretos / entornos (nunca dentro de la imagen)
.env
.env.*
!.env.example

# Datos locales en runtime (volúmenes nombrados en compose, no se bakean)
backups/
logs/
app/static/uploads/
box_crossfit.db

# Otros (contexto local / dev)
node_modules/
.git/
_mockups/
*.log
*.pid
dist/
