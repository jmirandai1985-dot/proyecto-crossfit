# URL del backend (FastAPI)
# Dev: vacío → usa el proxy de Vite (/api → http://localhost:8000).
# Docker/producción: vacío → MISMO ORIGEN; nginx proxea /api/ → backend:8000.
# Poner URL absoluta SOLO si el frontend y la API viven en hosts distintos.
VITE_API_URL=